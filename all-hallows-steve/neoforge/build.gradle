import org.gradle.jvm.tasks.Jar

plugins {
    id 'com.gradleup.shadow'
}

architectury {
    neoForge()
}

loom {
    accessWidenerPath = project(":all-hallows-steve:common").loom.accessWidenerPath

    neoForge {
        convertAccessWideners(tasks.named("shadowJar", Jar), "allhallowssteve.accesswidener")
    }
}

configurations {
    developmentNeoForge.extendsFrom common
}

dependencies {
    common(project(path: ':all-hallows-steve:common')) { transitive false }
    shadowBundle project(path: ':all-hallows-steve:common', configuration: 'transformProductionNeoForge')
}
