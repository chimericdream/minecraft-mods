accessWidener v2 official

accessible method net/minecraft/client/gui/screens/MenuScreens register (Lnet/minecraft/world/inventory/MenuType;Lnet/minecraft/client/gui/screens/MenuScreens$ScreenConstructor;)V
accessible class net/minecraft/client/gui/screens/MenuScreens$ScreenConstructor

accessible method net/minecraft/world/level/block/entity/BlockEntityType <init> (Lnet/minecraft/world/level/block/entity/BlockEntityType$BlockEntitySupplier;Ljava/util/Set;)V

accessible field net/minecraft/client/renderer/special/SpecialModelRenderers ID_MAPPER Lnet/minecraft/util/ExtraCodecs$LateBoundIdMapper;

# BlockModelGenerators has no public API for registering a composite (tinted model + special
# renderer) item model, only single-purpose private helpers like registerSimpleTintedItemModel — so
# DecoratedPumpkinBlockDataGenerator reaches the underlying output directly to register the decorated
# pumpkin's composite item model (see that class for why this can't just be hand-authored JSON:
# vanilla's own ItemInfoCollector.finalizeAndValidate always overwrites any item lacking a datagen'd
# model with a plain fallback, so a hand-authored file would be clobbered on the next datagen run).
accessible field net/minecraft/client/data/models/BlockModelGenerators itemModelOutput Lnet/minecraft/client/data/models/ItemModelOutput;

# CarvingStationScreen renders the output slot's carved pumpkin as a rotating preview by resolving its
# own ItemStackRenderState and adding a GuiItemRenderState directly, rather than going through
# GuiGraphicsExtractor#item (which always bakes ItemDisplayContext.GUI's fixed transform with no way to
# add our own per-frame rotation on top). guiRenderState has no public accessor for this.
accessible field net/minecraft/client/gui/GuiGraphicsExtractor guiRenderState Lnet/minecraft/client/renderer/state/gui/GuiRenderState;
