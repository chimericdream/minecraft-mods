import org.gradle.jvm.tasks.Jar

plugins {
    id 'com.gradleup.shadow'
}

architectury {
    neoForge()
}

loom {
    accessWidenerPath = project(":sneaky-tweaks:common").loom.accessWidenerPath

    neoForge {
        convertAccessWideners(tasks.named("shadowJar", Jar), "sneakytweaks.accesswidener")
    }
}

configurations {
    developmentNeoForge.extendsFrom common
}

dependencies {
    common(project(path: ':sneaky-tweaks:common')) { transitive false }
    shadowBundle project(path: ':sneaky-tweaks:common', configuration: 'transformProductionNeoForge')
}
