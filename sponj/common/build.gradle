architectury {
    common project(":sponj").enabled_platforms.split(',')
}

loom {
    accessWidenerPath = file("src/main/resources/sponj.accesswidener")
}

sourceSets {
    main {
        resources.srcDir 'src/main/generated'
        // optional: exclude the datagen cache files from the mod jar
        resources.exclude '.cache'
    }
}
