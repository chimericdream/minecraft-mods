// Standalone build: deliberately NOT part of the mod monorepo's settings.gradle. Nothing here is
// ever included by the root build, so `./gradlew build` at the repo root is unaffected.
rootProject.name = "mod-status-plugin"
