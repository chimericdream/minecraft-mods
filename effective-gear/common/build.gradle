architectury {
    common project(':effective-gear').enabled_platforms.split(',')
}

loom {
    accessWidenerPath = file("src/main/resources/effectivegear.accesswidener")
}

sourceSets {
    main {
        resources.srcDir 'src/main/generated'
        // optional: exclude the datagen cache files from the mod jar
        resources.exclude '.cache'
    }
}
