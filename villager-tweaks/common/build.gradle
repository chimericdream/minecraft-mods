architectury {
    common project(":villager-tweaks").enabled_platforms.split(',')
}

loom {
    accessWidenerPath = file("src/main/resources/villagertweaks.accesswidener")
}

sourceSets {
    main {
        resources.srcDir 'src/main/generated'
        // optional: exclude the datagen cache files from the mod jar
        resources.exclude '.cache'
    }
}
