architectury {
    neoForge()
}

loom {
    accessWidenerPath = project(":villager-tweaks:common").loom.accessWidenerPath
}

configurations {
    developmentNeoForge.extendsFrom common
}

dependencies {
    common(project(path: ':villager-tweaks:common', configuration: 'namedElements')) { transitive false }
    shadowBundle project(path: ':villager-tweaks:common', configuration: 'transformProductionNeoForge')
}

remapJar {
    atAccessWideners.add("villagertweaks.accesswidener")
}
