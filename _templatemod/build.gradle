allprojects {
    group = project(":template").maven_group
    version = project(":template").mod_version
}

subprojects {
    base {
        archivesName = project(":template").archives_name + "-" + project.name
    }
}
