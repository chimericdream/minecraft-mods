architectury {
    common project(':jdcrafte').enabled_platforms.split(',')
}

loom {
    accessWidenerPath = file("src/main/resources/jdcrafte.accesswidener")
}

sourceSets {
    main {
        resources.srcDir 'src/main/generated'
        // optional: exclude the datagen cache files from the mod jar
        resources.exclude '.cache'
    }
}
