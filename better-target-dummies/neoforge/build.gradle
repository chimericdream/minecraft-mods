import org.gradle.jvm.tasks.Jar

plugins {
    id 'com.gradleup.shadow'
}

architectury {
    neoForge()
}

loom {
    accessWidenerPath = project(":better-target-dummies:common").loom.accessWidenerPath

    neoForge {
        convertAccessWideners(tasks.named("shadowJar", Jar), "bettertargetdummies.accesswidener")
    }
}

configurations {
    developmentNeoForge.extendsFrom common
}

dependencies {
    common(project(path: ':better-target-dummies:common')) { transitive false }
    shadowBundle project(path: ':better-target-dummies:common', configuration: 'transformProductionNeoForge')
}
