architectury {
    common project(":archaeology-tweaks").enabled_platforms.split(',')
}

loom {
    accessWidenerPath = file("src/main/resources/archtweaks.accesswidener")
}

sourceSets {
    main {
        resources.srcDir 'src/main/generated'
        // optional: exclude the datagen cache files from the mod jar
        resources.exclude '.cache'
    }
}
