# Fabric gotchas

Runtime/build behaviors that are specific to the Fabric platform in this repo — things that work fine
on NeoForge (or work fine in general) but break, silently misbehave, or crash only under Fabric. Add
new entries here as they're found; `CLAUDE.md` just points here so day-to-day context doesn't carry the
full write-ups.

No Fabric-specific gotchas recorded yet — this file exists so the first one has a home. Follow the
same structure as `docs/NEOFORGE.md`: a heading per gotcha, then **Root cause** and **Fix** subsections
with concrete reference implementations.
