architectury {
    fabric()
}

loom {
    accessWidenerPath = project(":toy-box:common").loom.accessWidenerPath
}

configurations {
    developmentFabric.extendsFrom common
}

dependencies {
    common(project(path: ':toy-box:common')) { transitive false }
    shadowBundle project(path: ':toy-box:common', configuration: 'transformProductionFabric')
}
